{
  "name": "${project.artifactId}",
  "version": "${project.version}",
  "requires": {
    "java": "${java.version}",
    "plugin-api": "${pulse.plugin.api.version.required}"
  },
  "experts": [
    {
      "className": "com.microfocus.pulse.example.worddetector.WordDetectorPulseExpert"
    }
  ]
}
